<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:project.Models"
             xmlns:viewModels="clr-namespace:project.ViewModels"
             xmlns:converts="clr-namespace:project.Views.Converts"
             x:Class="project.Views.ToDoItem">

    <ContentView.BindingContext>
        <viewModels:ActiveToDoViewModel/>
    </ContentView.BindingContext>

    <ContentView.Resources>
        <ResourceDictionary>
            <converts:TimeSpanToLocalDateConverter x:Key="convert"></converts:TimeSpanToLocalDateConverter>
        </ResourceDictionary>

        <Style x:Key="frame-body-todo-style" TargetType="Frame" BasedOn="{StaticResource frame-body-todo-style}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding GetStatus}" Value="{Static local:StatusToDo.None}" TargetType="Frame">
                    <Setter Property="BorderColor" Value="{AppThemeBinding Light={StaticResource color-totdo-box-border-light}, Dark={StaticResource color-totdo-box-border-dark}}"></Setter>
                </DataTrigger>
                <DataTrigger Binding="{Binding GetStatus}" Value="{Static local:StatusToDo.Priority}" TargetType="Frame">
                    <Setter Property="BorderColor" Value="#55C32E"></Setter>
                </DataTrigger>
                <DataTrigger Binding="{Binding GetStatus}" Value="{Static local:StatusToDo.Urgently}" TargetType="Frame">
                    <Setter Property="BorderColor" Value="OrangeRed"></Setter>
                </DataTrigger>
                <DataTrigger Binding="{Binding GetStatus}" Value="{Static local:StatusToDo.Overdue}" TargetType="Frame">
                    <Setter Property="BorderColor" Value="Red"></Setter>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </ContentView.Resources>

    <ContentView.Content>
        <StackLayout Orientation="Vertical">
            <Frame Style="{StaticResource frame-body-todo-style}">
                <Frame Grid.Row="0" Style="{StaticResource frame-body-todo-style}" CornerRadius="23">
                    <Grid>
                        <Grid.Resources>
                            <Style x:Key="label-date-style" TargetType="Label" BasedOn="{StaticResource label-base-style}">
                                <Setter Property="Text" Value="{Binding EndDate, Converter={StaticResource convert}}"></Setter>
                                <Setter Property="HorizontalTextAlignment" Value="Center"></Setter>
                            </Style>
                        </Grid.Resources>

                        <Label Style="{StaticResource lable-warning-todo-style}">

                            <Label.Triggers>
                                <DataTrigger Binding="{Binding GetStatus}" Value="{Static local:StatusToDo.Overdue}" TargetType="Label">
                                    <Setter Property="IsVisible" Value="True"></Setter>
                                    <Setter Property="Text" Value="просрочено"></Setter>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding GetStatus}" Value="{Static local:StatusToDo.Urgently}" TargetType="Label">
                                    <Setter Property="IsVisible" Value="True"></Setter>
                                    <Setter Property="Text" Value="срочно"></Setter>
                                </DataTrigger>
                            </Label.Triggers>
                        </Label>
                        
                        <Grid Padding="22, 14, 14, 14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3.5*"></ColumnDefinition>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                            </Grid.ColumnDefinitions>

                            <StackLayout Grid.Column="0" Orientation="Horizontal" VerticalOptions="Center">
                                <BoxView HeightRequest="50" WidthRequest="50" CornerRadius="5" VerticalOptions="Center" BackgroundColor="{AppThemeBinding Light=WhiteSmoke, Dark={StaticResource color-conteiner-body-dark}}"></BoxView>
                                <Label Margin="4, 0, 0, 0" VerticalOptions="Center" Text="{Binding Title}" Style="{StaticResource label-normal-text-style}"></Label>
                                <Label VerticalOptions="Center" Text="{Binding Count}" Style="{StaticResource label-normal-text-style}"></Label>
                                <Label VerticalOptions="Center" Text="шт." Style="{StaticResource label-normal-text-style}"></Label>
                            </StackLayout>

                            <Grid Grid.Column="1">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsEmptySubToDos}" Value="true" TargetType="Grid">
                                                <Setter Property="Opacity" Value="0"></Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>

                                <Grid.Resources>
                                    <Style TargetType="Label" BasedOn="{StaticResource label-base-style}">
                                        <Setter Property="FontSize" Value="18"></Setter>
                                        <Setter Property="HorizontalTextAlignment" Value="Center"></Setter>
                                    </Style>
                                </Grid.Resources>

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="2"/>
                                    <RowDefinition Height="auto"/>
                                </Grid.RowDefinitions>

                                <Label Grid.Row="0" Style="{StaticResource label-date-style}"></Label>
                                <BoxView Grid.Row="1" CornerRadius="3" HorizontalOptions="Center" WidthRequest="20"></BoxView>
                                <StackLayout Grid.Row="2" Orientation="Horizontal" HorizontalOptions="Center">
                                    <Label Text="{Binding GetCountCompletedSubToDos}"></Label>
                                    <Label Text="из" Margin="-2, 0"></Label>
                                    <Label Text="{Binding GetAllCountSubToDos}"></Label>
                                </StackLayout>
                            </Grid>

                            <Label Grid.Column="1" 
                                   VerticalTextAlignment="Center" 
                                   FontSize="23"
                                   Opacity="0">

                                <Label.Style>
                                    <Style TargetType="Label" BasedOn="{StaticResource label-date-style}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsEmptySubToDos}" Value="true" TargetType="Label">
                                                <Setter Property="Opacity" Value="1"></Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Label.Style>
                            </Label>
                        </Grid>
                    </Grid>
                </Frame>
            </Frame>

            <Grid HeightRequest="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <BoxView Grid.Column="1" HeightRequest="1.5" CornerRadius="2"></BoxView>
            </Grid>
        </StackLayout>
    </ContentView.Content>
</ContentView>